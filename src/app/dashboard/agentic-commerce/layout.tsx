import type { ReactNode } from 'react'

export const metadata = {
  title: 'Agentic Commerce — Alignment AI',
  description: 'Agent Visibility Infrastructure — Register, Discover, Verify, and Track your brand across AI agents',
}

export default function AgenticCommerceLayout({ children }: { children: ReactNode }) {
  return <>{children}</>
}
